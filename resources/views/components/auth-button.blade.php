@props(['label' => 'Submit'])

<fieldset class="mt-4">
    <button type="submit"
        class="mt-10 w-full rounded-md bg-red-600 py-3 text-white transition hover:bg-red-700">{{ $label }}</button>
</fieldset>